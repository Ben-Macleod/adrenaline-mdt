import { Flex } from '@chakra-ui/react';
import * as React from 'react';
import type { NextPage } from 'next';

const Home: NextPage = () => {
	return <Flex>Landing Page for Adrenaline MDT</Flex>;
};

export default Home;
